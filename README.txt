First of all, you need to install all the requirements:
```
pip install -r requirements.txt
```

Then you can run the script with the following command:
```
python run_segmentation.py -input demo_dataset -output output
``` 

If you have doubts or suggestions, please contact me at: padillaperezjorge@gmail.com
